{
    "version": "0.2.0",
    "configurations": [
        
        {
            "name": "Start Env",
            "type": "python",
            "request": "launch",
            "program": "use_cases/roundtable/world_setup_n_launch_tree_of_thought.py",
            "console": "integratedTerminal",
            "env": {
                "PYTHONPATH": "${workspaceRoot}"
            },
        }
    ]
}